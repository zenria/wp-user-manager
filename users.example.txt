# Reference list: one e-mail address per line, one line per user.
# Blank lines and everything after `#` are ignored.
#
# Alternative addresses of the same person do NOT belong here: declare them in
# the alias file (see aliases.example.txt).

alice@example.com
bob@example.com
carol@example.com
